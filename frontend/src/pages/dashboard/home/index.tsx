import { Box } from "@mui/material";
import Typography from "@mui/material/Typography";

const HomePage = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <Typography variant="h2" sx={{ color: "white" }}>
        This is a Dashboard home
      </Typography>
    </Box>
  );
};

export default HomePage;
